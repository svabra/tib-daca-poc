/**
 * DaCa release version shown by the web applications.
 *
 * Keep this value in sync with the repository-level VERSION file through the
 * version-management script. It intentionally lives in the shared design
 * system so every DaCa UI renders the same release identifier.
 */
export const DACA_VERSION = '0.1.15' as const;
